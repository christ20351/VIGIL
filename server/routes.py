# this module has been split into the `routes` package.
# importing setup_routes from the package allows existing import paths
from routes import setup_routes

# nothing else here, the actual route definitions now live under routes/.*
